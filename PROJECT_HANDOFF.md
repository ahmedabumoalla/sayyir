# سجل مشروع سيّر — مرجع استكمال العمل

آخر تحديث: 2026-08-18

هذا الملف هو نقطة البداية عند استكمال العمل على المشروع. يجب قراءته قبل تعديل نظام الإشعارات أو الحجوزات أو الخدمات أو بيانات أرقام الجوال. لا تُكتب فيه مفاتيح API أو كلمات مرور أو Service Role فعلية.

## ملخص الحالة الحالية

- المشروع يعمل على Next.js 16.1.1 مع Turbopack.
- تم حل مشكلة Turbopack السابقة بحذف مجلد `.next` ثم إعادة تشغيل `npm run dev`.
- نجح `npx tsc --noEmit` بدون أخطاء.
- نجح `npm run build`، وتم بناء 103 صفحات ومسارات API.
- Green API متصل ويعمل، وتم تنفيذ اختبار إرسال حي إلى رقم الأدمن.
- رقم الأدمن المعتمد: `+966508424401`.
- اختبار الإرسال الحي أعاد `idMessage` بالقيمة `3EB02E9BF157967BCCEC9A`، وكانت حالته في Green API هي `sent`.
- تم تسجيل عملية الاختبار في جدول `admin_logs` مع إخفاء الرقم على صورة `***4401`.

## قرار مهم بخصوص Green API

الـInstance الحالي ورقم واتساب المتصل به مستخدمان مع أكثر من منصة، وليس مع سيّر فقط.

لذلك:

- لا تغيّر `webhookUrl` في Green API.
- لا تغيّر `webhookUrlToken`.
- لا تفصل الرقم ولا تعِد مسح QR.
- لا تفعّل أو تعطل إعدادات Webhook قبل معرفة المنصات الأخرى المرتبطة بالـInstance.
- `GREEN_API_WEBHOOK_TOKEN` اختياري حالياً ولا يلزم إضافته إلى Vercel.
- مسار Webhook الخاص بسيّر جاهز للمستقبل، لكنه غير مطلوب ما دام الـInstance مشتركاً.
- عند تخصيص Instance مستقل لسيّر مستقبلاً يمكن تفعيل Webhook وتتبع حالات `delivered` و`read` لحظياً.

الإرسال الحالي لا يعتمد على Webhook. يعتمد على `sendMessage` و`sendFileByUpload`، ويحفظ `idMessage` عند قبول Green API للرسالة.

## متغيرات البيئة المطلوبة

تُضاف في `.env.local` محلياً وفي Vercel، مع استخدام القيم الحقيقية هناك فقط:

```env
NEXT_PUBLIC_SITE_URL=https://sayyir.sa

GREEN_API_ID_INSTANCE=
GREEN_API_TOKEN_INSTANCE=
GREEN_API_API_URL=https://7107.api.greenapi.com
GREEN_API_MEDIA_URL=https://7107.api.greenapi.com

ADMIN_PHONE=+966508424401
INTERNAL_NOTIFICATION_SECRET=
```

اختياري مستقبلاً عند تخصيص Instance لسيّر:

```env
GREEN_API_WEBHOOK_TOKEN=
```

لا تجعل `GREEN_API_TOKEN_INSTANCE` أو `SUPABASE_SERVICE_ROLE_KEY` متغيرات تبدأ بـ`NEXT_PUBLIC_`.

## قرار تخزين رقم واتساب

- المصدر الرسمي الوحيد لرقم واتساب العميل أو المزود هو `profiles.phone`.
- لم تتم إضافة خانة مكررة مثل `whatsapp_phone`.
- الصيغة القياسية داخل قاعدة البيانات هي الصيغة الدولية: `+9665XXXXXXXX`.
- دوال التطبيع تدعم السعودية والإمارات والكويت والبحرين وعُمان وقطر ومصر.
- تقبل الدالة الإدخال المحلي السعودي مثل `05XXXXXXXX` وتحوله إلى `+9665XXXXXXXX`.
- قبل حفظ رقم جديد يتم استخدام Green API `checkWhatsapp` للتأكد أن الرقم مرتبط فعلياً بواتساب.
- إذا كان الرقم مستخدماً في حساب آخر تظهر رسالة واضحة ولا يتم تجاوزه.

نتيجة مراجعة البيانات وقت التنفيذ:

- جدول `profiles`: 41 حساباً.
- 15 رقماً أصبحت بصيغة دولية صحيحة.
- 24 حساباً بلا رقم؛ سيُطلب منهم إدخاله عند أول إجراء يحتاج واتساب.
- يوجد حسابان قديمان يؤدي تطبيع رقميهما إلى نفس الرقم؛ لم يتم دمج الحسابين أو حذف أي بيانات.
- جدول `provider_requests`: 19 طلباً، والأرقام القديمة قابلة للتطبيع عند الإرسال.
- محاولة تحديث بعض سجلات `provider_requests` القديمة اصطدمت بقاعدة/Trigger تمنع تسجيل المزود مرة أخرى؛ لذلك تُركت السجلات التاريخية دون تعديل، بينما كل الطلبات الجديدة تُحفظ بالصيغة الصحيحة.

## بوابة إدخال رقم واتساب مرة واحدة

الملفات الرئيسية:

- `components/WhatsAppPhoneGate.tsx`
- `app/api/profile/whatsapp/route.ts`
- `lib/whatsappProfile.ts`
- `lib/phone.ts`
- `app/layout.tsx`

السلوك:

1. قبل أن ينشئ العميل حجزاً أو يكمل الدفع، يتحقق النظام من `profiles.phone`.
2. قبل أن يرسل المزود طلب خدمة أو تعديل أو إيقاف أو قرار حجز أو سحب، يتم التحقق نفسه.
3. إذا كان الرقم صحيحاً ومخزناً لا تظهر أي نافذة.
4. إذا كان مفقوداً أو غير صالح تظهر نافذة إدخال.
5. بعد التحقق والحفظ لا يُطلب الرقم مرة أخرى.
6. التحقق موجود على الواجهة وعلى API في الخادم، فلا يمكن تجاوزه بطلب يدوي.

## مسارات الإشعارات المرتبطة

### طلبات انضمام المزود

- الطلب الجديد يرسل للأدمن التفاصيل ورابط المراجعة.
- قبول الأدمن يرسل للمزود تفاصيل القبول والدخول.
- رفض الأدمن يرسل للمزود سبب الرفض.
- الرقم يتحقق منه عند تقديم طلب الانضمام.
- الملفات:
  - `app/api/provider/register/route.ts`
  - `app/api/admin/approve/route.ts`
  - `app/api/admin/reject/route.ts`
  - `app/register/provider/page.tsx`
  - `app/admin/requests/page.tsx`
  - `app/admin/requests/[id]/page.tsx`

### الخدمات

- إضافة خدمة جديدة ترسل للأدمن.
- طلب تعديل خدمة يرسل للأدمن.
- طلب إيقاف مؤقت أو نهائي يرسل للأدمن.
- قرار الأدمن بالقبول أو الرفض يرسل للمزود مع السبب والتفاصيل.
- API يعيد حالة الإرسال الحقيقية، ولا يعرض نجاحاً وهمياً عند فشل واتساب.
- الملفات:
  - `app/api/provider/services/route.ts`
  - `app/api/provider/services/action/route.ts`
  - `app/api/provider/service-change-requests/route.ts`
  - `app/api/admin/services/action/route.ts`
  - `app/api/admin/service-change-requests/[id]/decision/route.ts`
  - `app/admin/services/AdminServicesReviewPage.tsx`

### الحجوزات

- عند إنشاء حجز يصل إشعار كامل للمزود.
- يصل للعميل تأكيد أن الطلب قيد المراجعة.
- قبول المزود يرسل للعميل رابط الدفع وموعد انتهاء المهلة.
- رفض المزود يرسل للعميل السبب وكامل بيانات الطلب.
- بعد الدفع يصل للعميل تأكيد وتذكرة وصورة QR.
- بعد الدفع يصل للمزود إشعار تأكيد الحجز والدفع.
- لا يتم قبول `userId` القادم من الواجهة دون مطابقته مع جلسة المستخدم.
- يتم التحقق من رقم العميل ورقم المزود قبل إنشاء الحجز وقبل الدفع.
- صفحات الدفع القديمة لم تعد تؤكد الحجز مباشرة؛ تحول المستخدم إلى `/checkout/[id]`.
- الملفات:
  - `app/api/bookings/create/route.ts`
  - `app/api/notifications/route.ts`
  - `app/api/provider/bookings/action/route.ts`
  - `app/api/paymob/initiate/route.ts`
  - `app/api/paymob/free-checkout/route.ts`
  - `app/api/paymob/webhook/route.ts`
  - `app/service/[id]/page.tsx`
  - `app/place/[id]/page.tsx`
  - `app/checkout/[id]/page.tsx`
  - `app/provider/bookings/page.tsx`

### طلبات السحب

- لم تعد صفحة المزود تكتب مباشرة في `payout_requests`.
- الطلب يمر عبر API موثق ويتحقق من:
  - جلسة المزود.
  - أن الحساب مزود فعّال.
  - رقم واتساب.
  - المبلغ والرصيد.
  - الآيبان السعودي بصيغة `SA` ثم 22 رقماً.
- يصل طلب السحب للأدمن عبر واتساب.
- قرار الاعتماد أو الرفض يرسل للمزود.
- الملفات:
  - `app/api/provider/register/payout/route.ts`
  - `app/api/admin/finance/action/route.ts`
  - `app/provider/finance/page.tsx`
  - `app/admin/finance/page.tsx`

## طبقة Green API وقوالب الرسائل

الملفات:

- `lib/greenApi.ts`: إرسال النصوص والملفات، التحقق من الأرقام، وإنشاء QR.
- `lib/whatsappNotifications.ts`: القوالب الاحترافية وروابط الإجراءات.
- `app/api/emails/send/route.ts`: إرسال البريد وواتساب مع نتيجة دقيقة.
- `app/api/notifications/route.ts`: تنسيق إشعارات الحجز للعميل والمزود.
- `lib/notificationAuth.ts`: حماية مسارات الإشعارات الداخلية.

كل رسالة تحتوي قدر الإمكان على:

- عنوان واضح.
- مرجع الطلب أو الحجز.
- الاسم والخدمة.
- التاريخ والوقت والكمية والسعر عند توفرها.
- سبب الرفض أو ملاحظة الأدمن.
- رابط مباشر لصفحة الإجراء.

رسالة الحجز المدفوع تستخدم `qrcode` لإنشاء صورة PNG وإرسالها عبر Green API.

## تتبع الإرسال

- `lib/whatsappAudit.ts` يسجل قبول أو فشل الإرسال في `admin_logs`.
- لا يتم تخزين الرقم كاملاً في سجل التدقيق؛ يُحفظ آخر أربعة أرقام فقط.
- `app/api/webhooks/green-api/route.ts` جاهز لتسجيل حالات `sent/delivered/read/failed` مستقبلاً.
- مع الـInstance المشترك الحالي لا تغيّر Webhook فقط من أجل سيّر.
- بدون Webhook يمكن تأكيد قبول Green API للرسالة والحصول على `idMessage`، لكن لا يوجد تسجيل لحظي دائم لـ`delivered/read`.

## مشاكل ظهرت وحلولها

### خطأ Turbopack: failed to create whole tree

الحل الذي نجح:

```powershell
Remove-Item -Recurse -Force .next
npm run dev
```

### اختفاء خانات تسجيل المزود

- كانت المشكلة مرتبطة ببيانات `registration_fields`.
- تمت استعادة 10 خانات التسجيل والتحقق من صفحة `/register/provider`.

### عدم وصول رفض طلب الانضمام

- كان مسار الرفض يغيّر حالة الطلب لكنه لا يتحقق بدقة من نتيجة واتساب.
- تم حفظ سبب الرفض، والتحقق من الرقم عبر `checkWhatsapp`، وإرجاع حالة إرسال صريحة.

### عدم وصول إضافة خدمة للأدمن

- تم ربط إنشاء الخدمة مباشرة بقالب واتساب للأدمن.
- تم اختبار إرسال إشعار خدمة فعلي سابقاً وظهر في Green API.

### رسائل نجاح وهمية

- بعض المسارات كانت تعتبر نجاح HTTP الداخلي نجاحاً حتى لو أعاد Green API `{ ok: false }`.
- تم تعديلها لتفحص `response.ok` و`body.success` ونتيجة واتساب نفسها.
- القرار أو الطلب قد ينجح في قاعدة البيانات، لكن الرسالة المعروضة تذكر بوضوح إذا فشل الإشعار.

### أرقام مثل `+96605...`

- تمت إضافة تطبيع يزيل الصفر المحلي بعد رمز الدولة.
- الصيغة المخزنة النهائية للسعودية هي `+9665XXXXXXXX`.

### تعارض رقمين في `profiles`

- قيد `profiles_phone_key` منع تحويل سجلين إلى الرقم الدولي نفسه.
- لم يتم حذف أو دمج الحسابات.
- البوابة تعرض رسالة أن الرقم مرتبط بحساب آخر وتطلب رقماً رسمياً مختلفاً.

## قواعد مهمة للاستكمال

- اقرأ هذا الملف قبل بدء أي تعديل متعلق بالحجوزات أو واتساب.
- لا تنشئ عمود واتساب جديداً دون حاجة؛ استخدم `profiles.phone`.
- لا تعِد إضافة Twilio SMS لمسارات واتساب.
- لا تثق في `userId` أو `providerId` القادم من الواجهة؛ طابقه مع جلسة Supabase على الخادم.
- لا تجعل نجاح تعديل قاعدة البيانات يعني تلقائياً نجاح الإشعار.
- عند إضافة نوع طلب جديد:
  1. تحقق من جلسة المستخدم على الخادم.
  2. استخدم `ensureProfileWhatsApp`.
  3. نفذ العملية في قاعدة البيانات.
  4. أضف قالباً احترافياً في `whatsappNotifications.ts`.
  5. افحص نتيجة Green API.
  6. أرجع `notificationStatus` ورسالة دقيقة للواجهة.
  7. أضف رابط إجراء مباشر.
- لا ترسل أسرار Green API إلى المتصفح.
- لا تغيّر إعدادات Green API للـInstance المشترك دون مراجعة تكاملات المنصات الأخرى.

## خطوات التحقق عند أي تعديل قادم

```powershell
npx tsc --noEmit
npm run build
```

ثم نفّذ اختباراً واحداً فقط على رقم معروف، وتأكد من:

- أن HTTP ناجح.
- أن `success` يساوي `true`.
- وجود `idMessage`.
- وجود سجل `whatsapp_accepted` في `admin_logs`.
- عدم تسجيل رقم المستلم كاملاً.

لا ترسل اختبارات جماعية للعملاء أو المزودين.

## نقاط مستقبلية اختيارية

- تخصيص رقم وGreen API Instance مستقل لسيّر.
- عندها تفعيل Webhook الآمن لتتبع `delivered/read/failed` لحظياً.
- معالجة الحسابين القديمين اللذين يشتركان في رقم واحد بعد التواصل مع أصحابهما.
- إضافة لوحة داخل الأدمن لعرض سجلات إرسال واتساب بدلاً من قراءتها من `admin_logs` مباشرة.
- إضافة آلية إعادة محاولة منظمة للرسائل الفاشلة، مع منع الإرسال المكرر باستخدام `idMessage` ومرجع الطلب.
