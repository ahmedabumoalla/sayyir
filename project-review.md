# مراجعة عامة للمشروع

هذا الملف لتوثيق سير العمل بعد كل رد/تعديل.

## آخر حالة محدثة
- التاريخ: 2026-08-19
- الحالة: تم تصحيح كل معالم الصفحة (الـ12 سجل) في قاعدة البيانات حيث كانت البيانات النصية تظهر كـ `????` بعد التعديل السابق.
- الملفات المعدلة: لا يوجد تعديل برمجي جديد في الملفات؛ تم إجراء تحديثات مباشرة على جدول `places` في قاعدة بيانات Supabase.

## قالب التحديث الدوري
- المشكلة/المطلوب:
  توافق ترتيب المعالم الظاهر في واجهة المستخدم مع ترتيب الشيت، مع المحافظة على بيانات الصور والمواقع الحالية.
- الحل المتبع:
  1) إنشاء ملف ترتيب مركزي `lib/landmarkOrder.ts` يحوي ترتيب المعالم حسب الشيت.
  2) إضافة دالة فرز عامة `sortPlacesByLandmarkSheet`.
  3) ربط دالة الفرز في صفحات الواجهة التي تعرض المعالم:
     - صفحة المعالم `/landmarks`
     - الصفحة الرئيسية لعرض قسم أبرز المعالم.
  4) لا توجد تغييرات في قاعدة البيانات؛ الترتيب تم على مستوى العرض فقط.
- الملفات المعدلة:
  - `lib/landmarkOrder.ts` (إضافة)
  - `app/landmarks/page.tsx`
  - `app/page.tsx`
- الاعتماد/المرجع داخل المشروع:
  ترتيب الشيت المرفق + طلبك بإعادة الترتيب فقط + القواعد الجديدة في `AGENT.md`.

## تحديث إضافي (2026-08-19): إصلاح سجل `سوق محايل الشعبي` في قاعدة البيانات
- المشكلة: بعد تنفيذ إعادة ترتيب الواجهة، تبين أن سجل `سوق محايل الشعبي` لا يزال يحمل وصفًا وإحداثيات غير منطقية (تم تبادل بياناته مع سجل آخر في نفس الجدول).
- طريقة الحل:
  1) أخذنا نقطة رجوع Git عبر commit احتياطي: `3c941a6` (قبل أي تعديل جديد).
  2) طابقنا اسم المعلم مع مصدر الشيت ورقم السطر المقابل (`row 51`).
  3) استخرجنا رابط Maps من الشيت وحوّلناه لإحداثيات `lat=18.5467437` و`lng=42.0486282`.
  4) نفّذنا تحديث مباشر عبر REST API إلى جدول `places` للسجل ذات المعرف:
     `6a9e7f93-6807-4446-836d-d357d0b407d5`
     مع تصحيح:
     - `description` إلى نص من الشيت.
     - `lat` و`lng` إلى القيم الصحيحة.
- النتيجة: تم تحديث السجل بنجاح وأصبح متوافقًا مع المصدر.

## تحديث إضافي (2026-08-19): إزالة ظاهرة `????` وإعادة تطابق النصوص مع الشيت
- المشكلة: ظهر أن معظم حقول `description` في `places` تتحول لعلامات استفهام عند القراءة، وبالتالي لا تظهر البيانات بشكل صحيح على الواجهة.
- طريقة الحل:
  1) قرأنا الشيت المباشر من Google مرة ثانية بصيغة CSV بشكل UTF-8 (`export?format=csv`) مع parser داعم للتعابير المقتبسة والحذف التدريجي للسطر.
  2) ربطنا كل اسم في DB مع السطر المقابل في الشيت.
  3) استخرجنا الإحداثيات من روابط Maps.
  4) نفّذنا `PATCH` لكل سجل موجود في DB (12 سجلًا) لتحديث:
     - `description` الكامل الصحيح
     - `lat` و`lng`
- النتيجة: تم تحديث كل السجلات بنجاح، وبالفحص مباشرة عبر Supabase REST رجعت النصوص العربية مقروءة (بدون علامات `????`) لجميع العناصر المذكورة.

## ملاحظات التزام
- تشغيل المشروع الكامل غير مفعل (كما هو مطلوب).
- تم الالتزام بالنسخ الاحتياطي المسبق قبل التعديل عبر commit.

## تحديث إضافي (2026-08-19): استرجاع العملاء من وضع المؤرشفة + تنفيذ منطق رقم الواتساب

- المشكلة:
  - تم الإبلاغ أن جميع العملاء ظهروا كمؤرشفين بعد ربط رقم الواتساب.
  - فحصنا أن مسار الأرشفة (is_deleted) في الكود لا يتغيّر تلقائيًا داخل مسارات الواتساب؛ أي أنه لا يوجد ربط مباشر بين فحص رقم الواتساب ووضع `is_deleted = true`.
  - الفحص أظهر أن عدد العملاء المؤرشفين كان موجودًا في جدول `profiles` (`is_deleted = true` و `is_provider = false`) قبل الاسترجاع.

- الإجراء الذي تم على قاعدة البيانات مباشرة (قبل أي تعديل إضافي على الكود):
  - أخذنا نسخة احتياطية:
    - `backups/app_api_admin_users_action_route.ts.bak.<timestamp>`
    - `backups/project-review.md.bak.<timestamp>`
    - `backups/components_WhatsAppPhoneGate.tsx.bak.<timestamp>`
  - تم تنفيذ استرجاع جماعي:
    - `PATCH` على `profiles` حيث:
      - `is_provider = false`
      - `is_deleted = true`
    - تحديث القيم إلى:
      - `is_deleted = false`
      - `deleted_at = null`
      - `updated_at = الآن`
  - التأكيد:
    - قبل الاسترجاع: `26` عميل إجمالي / `10` عميل مؤرشف.
    - بعد الاسترجاع: `0` عميل مؤرشف.

- الاعتماد داخل المشروع:
  - جدول المستخدمين الأساسي: `profiles`
  - منطق الواتساب الحالي يعتمد على:
    - `/api/profile/whatsapp`
    - `components/WhatsAppPhoneGate.tsx`
    - `lib/whatsappProfile.ts`
  - مسارات الحجز/الخدمات التي تعتمد عليها الواجهة لا تزال تطلب رقم واتساب عبر بوابة واحدة قبل تنفيذ الإجراء.

- حالة التنفيذ:
  - تم تنفيذ التعديل الفعلي المطلوب على قاعدة البيانات.
  - لم يُعدّل مسار الكود الأساسي لتغييرات أوسع بعد هذه الخطوة.

## تحديث إضافي (2026-08-19): استرجاع حالة “موقوف” الرسمي بعد إرجاع الأرشفة

- المشكلة:
  - بعد رفع `is_deleted` للرجوع، ظلت حالة العملاء تظهر “موقوف”.
  - فحصنا أن سبب ذلك لم يكن الأرشفة، بل:
    - وجود `is_blocked` و/أو `is_banned` مفعّلة في `profiles`.
    - وجود `banned_until` مستقبلية في `auth.users` لبعض الحسابات.

- الإجراء المنفذ:
  1) تحقق أولي:
     - عدد العملاء مع حالة إيقاف (`is_banned=true` أو `is_blocked=true`) كان `13`.
     - عدد العملاء المؤرشفين بعد الخطوة السابقة أصبح `0`.
  2) استرجاع رسمي في `profiles`:
     - فُصلت جميع العملاء المتوقفين (`is_provider=eq.false` و`is_blocked=eq.true`) إلى:
       - `is_blocked = false`
       - `is_banned = false`
       - `updated_at = الآن`
  3) استرجاع رسمي في Auth:
     - تم تشغيل `auth.admin.updateUserById(..., { ban_duration: 'none' })` للحسابات التي كانت `banned_until` فيها مفعلة ضمن نفس المجموعة.

- النتيجة:
  - التحقق النهائي أعاد:
    - `is_blocked` = `0`
    - `is_banned` = `0`
    - `auth_banned_after` = `0`

- اعتماد المشروع:
  - `profiles` (حقول `is_deleted`, `is_banned`, `is_blocked`)
  - Supabase Auth Admin API (رفع الحظر الرسمي عبر `ban_duration: 'none'`).

## تحديث إضافي (2026-08-22): إصلاح نشر إشعارات الأدمن عبر Green API

- المشكلة:
  - النسخة المحلية ترسل إشعارات واتساب عبر Green API، بينما `main` المنشور كان لا يزال يرسل SMS عبر Twilio.
  - مسار webhook الخاص بـ Green API لم يكن موجودًا في النسخة المنشورة.
- طريقة الحل:
  1) إنشاء نقطة Git مرجعية قبل العمل بالمعرف `6a2ad1b`.
  2) جلب أحدث `origin/main` ودمج تحديث التحقق من الخصومات مع حماية الدفع وإشعارات واتساب المحلية.
  3) الحفاظ على `lib/greenApi.ts` و`lib/whatsappNotifications.ts` ومسار webhook، مع إبقاء التحقق من المستخدم وأرقام واتساب في مسارات الحجز والدفع.
  4) التحقق عبر `npx tsc --noEmit` بنجاح.
- الملفات المتأثرة مباشرة أثناء حل الدمج:
  - `app/api/bookings/create/route.ts`
  - `app/api/paymob/initiate/route.ts`
  - `app/api/paymob/free-checkout/route.ts`
  - `app/checkout/[id]/page.tsx`
  - ملفات تحديث الخصومات الواردة من `origin/main`.
- أساس القرار:
  - فحص Green API أكد أن الـ instance مصرح وأن رقم الأدمن موجود على واتساب.
  - فحص GitHub والإنتاج أثبت أن سبب غياب الإشعارات هو اختلاف النسخة المنشورة عن تكامل Green API المحلي.
  - أضيف `GREEN_API_TOKEN_INSTANCE` كخيار مصادقة احتياطي للـ webhook لأن سر webhook الموجود في Vercel لا يطابق البيئة المحلية؛ يبقى التوكن منقولًا في ترويسة المصادقة عبر HTTPS ولا يوضع في الرابط.
  - التحقق يقبل أي توكن مضبوط وصالح بدل إعطاء الأولوية لقيمة واحدة، لضمان التوافق مع سر Vercel القديم دون تعطيله.
  - عبر Vercel CLI تم توحيد متغيرات Production التالية مع البيئة المحلية العاملة، دون طباعة قيمها: `GREEN_API_ID_INSTANCE` و`GREEN_API_TOKEN_INSTANCE` و`GREEN_API_API_URL` و`GREEN_API_MEDIA_URL` و`ADMIN_PHONE` و`INTERNAL_NOTIFICATION_SECRET`، وإضافة `GREEN_API_WEBHOOK_TOKEN`.
