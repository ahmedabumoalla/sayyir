# AGENT Instructions

هذه التعليمات تُطبَّق على أي عمل داخل هذا المشروع من الآن فصاعدًا.

## 1) نطاق العمل والتعديلات
- لا يُنفَّذ أي تعديل إلا بناءً على طلب واضح ومحدد من المستخدم.
- لا يُغير شيء خارج نطاق المطلوب في المطلوب نفسه.
- لا تُنفّذ تعديلات تقديرية أو إضافات غير مطلوبة.

## 2) النسخ الاحتياطية قبل العمل
- قبل أي تعديل في الملفات: الالتزام بعمل نقطة مرجعية في Git.
- إذا كانت الـ repo غير نظيفة، لا تُجري تغييرات جديدة حتى توضع حالة مرجعية واضحة للمستقبل.
- القاعدة: 
  - `git add -A`
  - `git commit -m "chore: backup before assistant changes"`
- إذا تعذر التنفيذ (غياب git أو قيود بيئة)، يدوَّن ذلك في `project-review.md` بشكل واضح قبل المتابعة.

## 3) التشغيل والاختبار
- لا يُشغَّل المشروع كاملاً (npm run dev / pnpm dev / yarn dev ... ) إلا إذا طلب المستخدم ذلك صراحة.
- لا تُعدُّل إعدادات البيئية (ENV/Secrets) إلا بتوجيه مباشر.

## 4) تتبع المراجعة العامة بعد كل رد/تعديل
- بعد أي رد يتضمن إجراء تغييرات، يجب تحديث `project-review.md` فقط ليتضمن:
  - المشكلة/المهمة المطلوبة.
  - الطريقة المتبعة للحل.
  - الملفات التي عُدلت.
  - الأساس الذي اعتمد عليه (سبب القرار).
- التحديث في `project-review.md` يكون مختصرًا وواضحًا.

## 5) قيود قراءة الملفات
- لا يُقرأ المشروع كاملًا بشكل شامل ما لم يطلب المستخدم.
- يُطالع فقط الملفات اللازمة للمهمة المحددة.

## 6) التزامي أنت كـ AI Assistant
- الالتزام بهذه القواعد هو الأعلى أولوية في هذا المشروع.
- عند تعارض أي طلب مع هذه التعليمات، يتم طلب توضيح المستخدم قبل التنفيذ.
